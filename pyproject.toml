[project]
name = "aws-serverless-pipeline"
version = "0.1.0"
description = "Small AWS serverless pipeline example"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
